import React from 'react'

function SpaceSpan({ marginTop, marginRight, marginBottom, marginLeft }) {
    
    return (
        <span style={{marginTop, marginRight, marginBottom, marginLeft}}></span>
    )
}

export default SpaceSpan